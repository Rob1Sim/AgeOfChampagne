{% extends 'base.html.twig' %}

{% block title %} {{ vigneron.getNom()}} {{ vigneron.getPrenom()}} {% endblock %}

{% block body %}
    <div id ="show-vigneron-body">
        <main id="show-vigneron">
            <section id="vigneronInfos">
                <aside>
                    <h2 class="handWriting-text">{{ vigneron.getNom()}} {{ vigneron.getPrenom()}}</h2>
                    <p class="myriad-text">Le vigneron <strong>{{ vigneron.getNom()}} {{ vigneron.getPrenom()}}</strong> est situé dans la ville de <i>{{ vigneron.getVille()}}</i>
                    dont l'adresse est <i>{{ vigneron.getCodePostal()}}</i> <i>{{ vigneron.getAdresse()}}</i></p>
                </aside>
            </section>

            <section id="cruInfos">
                <h2 class="handWriting-text">{{ vigneron.cru.libelle }}</h2>
                <p class="myriad-text">Le cru correspondant à ce vigneron est <strong>{{ vigneron.cru.libelle }}</strong>. Les informations sur les horaires sont <i>{{ vigneron.cru.horaire }}</i>.</p>
                <p class="myriad-text">Voici les informations complétaires sur le cru : <i>{{ vigneron.cru.infos }}</i></p>
            </section>

            <section id="produitInfos">
                Liste des produits vendus par le vigneron :
                <table class="tableau_prod">
                    <tr>
                        <td><strong>Nom du produit</strong></td>
                        <td><strong>Prix du produit</strong></td>
                    </tr>
                    {% for prod in vigneron.produit %}
                        <tr>
                            <td><h2 class="handWriting-text">{{ prod.libelle }}</h2></td>
                            <td><p class="myriad-text">{{ prod.prix }}</p></td>
                        </tr>
                    {% endfor %}
                </table>
                <h2 class="handWriting-text">{{ vigneron.produit.libelle }}</h2>
                <p class="myriad-text">Le produit vendu par le vigneron aura pour prix <i>{{ vigneron.produit.prix }}</i></p>
                <!--
                afficher le prix si l'utilisateur est majeur
                -->
            </section>

            <footer id="show-vigneron-footer"></footer>
        </main>

    </div>

{% endblock %}