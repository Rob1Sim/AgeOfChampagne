<div class="gaucheCarte"><ul>
    {% for carte in liste %}
        <div class="carte">
            <a href=" {{ path('app_carte_show', {'id': carte.id}) }}"><img src="/uploads/img/{{carte.contenuImage}}" alt="{{ carte.nom }}"></a>
            <div class="droiteInfoCarte"><p class="carte-name">{{ carte.nom }}</p>
            <p class="carte-type">{{ carte.type }}</p>
            <p class="carte-cru">{{ carte.cru }}</p></div>
        </li>
    {% endfor %}
</ul></div>