{% extends 'base.html.twig' %}

{% block body %}
    <div id ="show-carte-body">
        <main id = "show-carte">
            <section id="carteInfo">
                <img src="/uploads/img/{{carte.contenuImage}}" alt="Carte du vignoble">
                <aside>
                    <h3>{{ carte.nom }}</h3>
                    <p>Le <strong>{{ carte.nom }}</strong> est un <i>{{ carte.type }}</i> qui produit un cru <i>{{ carte.cru }}</i>
                        dans la région : {{ carte.region }}, elle représente une supèreficie de {{ carte.superficie }} m&#178; </p>

                </aside>
            </section>
            <section>

            <!--
            Cartes intéractive
            <section>
                <h3>Coordonées :</h3>
            </section> -->
                <h3>Partenaires : </h3>
            </section>
        </main>
    </div>

{% endblock %}

{% block title %}

{% endblock %}

